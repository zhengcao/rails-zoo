<h1> <%= @animal.name %></h1>

<p>
  <%= @animal.name %> is a <%= @animal.species %> who is <%= @animal.age %>
  <% if @animal.age <= 1 %>
    year old.
  <% else %>
    years old.
  <% end %>
</p>

<%= link_to @animal.image do %>
  <%= image_tag @animal.image, :size => "400x260", alt: @animal.species, class: "image" %>
<% end %>
<br>
<%= link_to "Edit", edit_animal_path(@animal) %>
<%= link_to "Delete", animal_path(@animal), :data => {:confirm => 'Are you sure?'}, method: :delete %>
